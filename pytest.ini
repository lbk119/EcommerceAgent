[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    e2e: requires a running gateway and Python brain
    evals: quality evaluation tests; mock mode runs without external LLMs
    security: local red-team and policy boundary tests
    perf: performance scenario assets and lightweight checks
    chaos: chaos experiment contract checks
filterwarnings =
    ignore::DeprecationWarning