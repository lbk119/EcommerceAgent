{
  "version": "1.0.0",
  "title": "Brain API returns 5xx",
  "description": "Inject Brain API 5xx responses and verify gateway envelope/proxy behavior plus task failure recovery.",
  "steady-state-hypothesis": {
    "title": "gateway health ok and tasks do not stay running",
    "probes": [
      {"type": "probe", "name": "health-ok", "provider": {"type": "http", "url": "${GATEWAY_URL:-http://127.0.0.1:9090}/health", "timeout": 5}, "tolerance": {"type": "jsonpath", "path": "$.status", "expect": "ok"}}
    ]
  },
  "method": [],
  "rollbacks": []
}